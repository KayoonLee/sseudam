<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.pet.sseudam.dao.TempAdminDao">
    <select id="admin_list" resultType="Member">
        select * from member
    </select>

    <select id="adminSelect" resultType="member">
        select member.*, pet.* from member join pet ON member.m_id = #{m_id}
            and pet.g_id = #{m_id}
    </select>

    <select id="petSelect" resultType="pet">
        select member.*, pet.* from member join pet ON member.m_id = #{m_id}
            and pet.g_id = #{m_id}
    </select>


</mapper>