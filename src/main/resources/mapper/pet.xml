<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="com.pet.sseudam.dao.PetDao">
    <!--반려동물 목록-->
    <select id="p_list" parameterType="pet" >
        select * from pet

    </select>

    <!--반려동물 인서트-->


    <!-- 반려동물 몇 마리-->
    <select id="getTotal" resultType="int">
        select count(*) from pet
    </select>

    <!-- 반려동물 상세정보 -->
    <select id="p_select" parameterType="int" resultType="pet">
        select * from pet where p_id=#{p_id}
    </select>

    <!-- 반려동물 정보수정 -->
    <update id="p_update" parameterType="pet">
        update pet set animal=#{animal}, birth=#{birth}, kind=#{kind}, breed=#{breed}, feature=#{feature}, gender=#{gender}, neutering=#{neutering}
        where p_id=#{p_id}
    </update>

    <!-- 반려동물 추가 -->
    <insert id="p_insert" parameterType="pet">
        insert into pet (p_id, g_id, animal, birth, kind, breed, profile, feature, gender, neutering, state, profile_num )
                values
       ( #{p_id},#{g_id}, #{animal}, #{birth}, #{kind}, #{breed}, '사진아직', #{feature}, #{gender}, #{neutering}, #{state}, #{profile_num} )
    </insert>

</mapper>
